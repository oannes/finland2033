# P2-O3 — The Split State ("Seventeen decisions, zero decision")

**Fires on:** any mixed trio or H-majority (combo rows per table; render the row note). **Index effects:** RES −0.4, LEG −0.4, PRO −0.4, POLL −5.

## What happens (2029)

No national line survives contact with the institutions. Ministries attest, counties migrate, agencies hedge. By December Finland runs public services on three model tiers under two legal regimes, and the org chart is the architecture.

Every individual choice is defensible. The combination cannot answer the one question Brussels and Washington both ask: what does Finland run on? The costs are quiet and everywhere, and a 30% overhead for running everything twice gets a parliamentary question and no owner.

That winter's national defence course ran its exercise on the assumption that a crisis would not wait for the procurement disputes to resolve. Nobody called the assumption unfair.

The split state's ledger, 2029: sovereign share {sov_share}%, trust down to {trust}%, approval at {poll}%, and a 30% optionality tax nobody's budget names. The gap: {cap_gap} months and widening.

## Flags
`STACK=split` · `CRISIS_LEG=damaged` (the rule's floor; a split state cannot manage a gate)

## Data

```yaml
node: P2-O3
year: 2029
youth_u: inherit+0.4
care_gap: inherit+1
pub_ai: inherit-1
sov_share: inherit+3
trust: inherit-3
compute_mw: inherit+20
adjustments:
  - if: RETRAIN=broad
    youth_u: -0.5
  - if: MEGAPROJECT=pilot
    trust: -2
  - if: SECURE_ARCH=yes
    trust: +1
```

## Persona implications

- **MARJA (M2):** Her benefits pipeline must now reconcile three engines. The staged plan becomes a translation layer; the working week becomes reconciliation meetings. Coming back keeps needing to be justified, mostly to herself.
- **EETU (E3):** Integration consulting booms; the split state bills beautifully. He glues incompatible stacks for public clients and writes the same adapter three times under three procurement codes.

## Modifier hooks

- **SAK-A:** The counterparty question goes unanswered seventeen different ways.
- **SAK-B:** There is no single annex to negotiate; labour faces a fog instead of a counterparty.
- **SAK-H:** Sitting out the stack fight looks prescient and feels like absence.
- **TI-A:** The sovereign order book gets a third of its volume and none of its certainty.
- **TI-B:** The lane letters are answered agency by agency; exporters navigate by rumor.
- **TI-H:** The public split becomes the reference case for why the association split.
- **AALTO-A:** Certification has no single object; the sprint becomes a taxonomy (TALENT_PIPE=strong, misallocated).
- **AALTO-B:** The frontier labs run; their graduates join the integration boom instead.
- **AALTO-H:** Measuring both tiers was the right call for a country running three.
- **HVK-A:** Doctrine has no single stack to apply to. The exercises are postponed for scoping.
- **HVK-B:** Conditional access negotiated seventeen times, enforced zero.
- **HVK-H:** The dependency map is finished, classified, and the year's most honest document.
